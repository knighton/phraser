dependencies:
  cache_directories:
    - "env"
  pre:
    - sudo apt-get update
    - sudo apt-get install g++-4.7
  override:
    - make extras

test:
  override:
    - make test
  post:
    - make package

general:
  artifacts:
    - "cover"
    - "dist"
